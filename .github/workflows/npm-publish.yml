name: npm-publish

# Family convention: release/published triggers the publish (see workspace AGENTS.md).
# Trusted publishing (OIDC) — npmjs.com package settings declare this file as trusted publisher. — no npm token needed:
# npmjs.com package settings must declare this workflow file name
# (npm-publish.yml) as a trusted publisher for the matching repo.
on:
  release:
    types: [published]

permissions:
  id-token: write # Required for OIDC (npm trusted publishing)
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24 # npm CLI 11.x — OIDC trusted publishing requires npm >= 11.5.1
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: npm publish (OIDC)
        run: npm publish --access public
